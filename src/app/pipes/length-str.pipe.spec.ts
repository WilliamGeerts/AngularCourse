import { LengthStrPipe } from './length-str.pipe';

describe('LengthStrPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthStrPipe();
    expect(pipe).toBeTruthy();
  });
});
